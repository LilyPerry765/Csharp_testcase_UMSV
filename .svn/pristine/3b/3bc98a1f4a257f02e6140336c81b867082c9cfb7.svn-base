using System.Windows;
using Plugin.Mailbox.ViewModels;

namespace Plugin.Mailbox.Views
{

    public partial class PublicMailboxEditView : Window
    {
        public PublicMailboxEditView(MailboxEditViewModel viewModel)
        {
            InitializeComponent();
            this.DataContext = viewModel;
        }

    }
}
