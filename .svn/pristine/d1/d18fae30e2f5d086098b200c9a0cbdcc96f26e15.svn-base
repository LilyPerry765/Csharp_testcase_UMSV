using System.Windows;
using Plugin.Mailbox.ViewModels;

namespace Plugin.Mailbox.Views
{

    public partial class MailboxEditView : Window
    {
        public MailboxEditView(MailboxEditViewModel viewModel)
        {
            InitializeComponent();
            this.DataContext = viewModel;
        }

    }
}
